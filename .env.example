DATABASE_URL="file:./dev.db"

# Vercel production (Neon + Blob). Local can keep SQLite above.
# DATABASE_URL="postgresql://..."
# DIRECT_URL="postgresql://..."   # Neon unpooled URL for prisma db push
# BLOB_READ_WRITE_TOKEN=""
